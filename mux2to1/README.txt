
mux design:
	a - first input 8 bit num
	b - sec input 8 bit num
	sel - selector 2 bit num
	out - output of the mux

mux test bench:
	defined the inputs call for instance of the mux
	print on the screen the resaults
	starting a loop that randomly selct num and give them to the mux

